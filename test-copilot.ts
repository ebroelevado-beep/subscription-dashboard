import { createOpenAI } from '@ai-sdk/openai';
import { generateText } from 'ai';

async function main() {
  // We need to fetch the internal token first using a github oauth token
  // Let's pretend we have one. Oh wait, I don't have a valid github oauth token for copilot right now that I can hardcode.
  console.log("Just checking imports");
}
main();
